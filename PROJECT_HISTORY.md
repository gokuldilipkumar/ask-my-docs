# Project History

**Project Goal:** Build a production-shaped, domain-specific RAG system ("Ask My Docs") over the FAA Airplane Flying Handbook (FAA-H-8083-3C) — hybrid retrieval (BM25 + dense) fused via RRF, cross-encoder reranking, grounded generation with verified citations via the Anthropic API, an evaluation harness with a CI quality gate, and full tracing/observability via Langfuse. The pipeline is ~20% of the work; proving it works (and catching regressions) is the point. Full spec: `production-rag-build-prompt.md`.

**Target Audience:** Portfolio piece for hiring managers / potential clients — demonstrates production RAG engineering (hybrid retrieval, reranking, citation verification, eval-gated CI, observability), not just a working demo. README, eval report, and architecture diagram should be presentation-quality and explain the reasoning behind architecture choices, not just what was built.

---

## Core Philosophy

### The Problem This Solves

Most RAG demos stop at "it retrieves and generates an answer." That's not production-shaped — there's no way to know if it's *right*, no way to catch regressions when a prompt or chunking strategy changes, and no visibility into cost/latency/failure rate in production.

**Before this project:**
- No verifiable way to trust a RAG answer's citations
- No regression detection when retrieval/prompt/config changes
- No observability into per-request cost, latency, or failure modes

**After this project:**
- Every citation is verified against its source chunk, not just formatted
- CI fails automatically if a change regresses retrieval quality
- Every query produces a traced, cost/latency/coverage-instrumented request

---

## Key Design Decisions

Full one-line log in `.agent/decisions.log`; full reasoning/explanations in `LEARNING_NOTES.md`. Highlights: `sentence-transformers` over `fastembed` (CPU speed claims didn't hold up in research), `lancedb` for the vector store, `pymupdf` (not plain text extraction) for ingestion since subsection headings need font metadata to detect reliably, content-hash chunk IDs (not row-index) so re-ingestion doesn't break citations.

---

## Session Log

| Date | What I Did | Key Decisions | Next Steps |
|------|-----------|---------------|------------|
| 2026-07-10 | Read seed spec (`production-rag-build-prompt.md`), proposed initial repo structure/library stack, set up SDLC scaffolding (`.agent/workflows/`, `skills/`, `CLAUDE.md`, `PROJECT_HISTORY.md`) | Audience confirmed as portfolio/hiring-manager facing | Resume `/brainstorm`: library build-vs-borrow research, PRD sections (Architecture, Components, Data Flow, Error Handling), then inspect the actual PDF to propose chunking strategy + sample eval questions |
| 2026-07-10 | Completed `/brainstorm`: library build-vs-borrow research, full PRD (Architecture/Components/Data Flow/Error Handling), inspected the actual PDF (406 pages, 18 chapters, single-column, dual page-numbering, 279 figure captions, zero table captions) to ground the chunking strategy and 8 sample eval questions in real structure. Wrote PRD to `docs/plans/2026-07-10-ask-my-docs-design.md`, decisions to `.agent/decisions.log`, reasoning to `LEARNING_NOTES.md` | Chunking uses pymupdf font metadata (not regex) for subsection detection; content-hash chunk IDs; local eval-response cache + daily cost total added for cost control | Run `/plan` to break the PRD into TDD-ready, bite-sized implementation tasks |
| 2026-07-10 | Session wrap-up: added `.gitignore` (excludes the 273MB PDF and local settings), `git init`, initial commit of all scaffolding + design docs (33 files) | Repo now version-controlled locally; no remote configured yet | **Resume here next session: run `/plan`** against `docs/plans/2026-07-10-ask-my-docs-design.md` to produce the TDD implementation plan |
| 2026-07-11 | Ran `/plan`. Verified dev environment (no system Python; `uv` already manages Python 3.11.8 — adopted `uv` over `pip`/`venv`/`requirements.txt`). User chose scoped plan: full TDD chunk detail (exact test code, RED/GREEN steps) for Block 0 (env/config) + Block 1 (ingestion) only; Blocks 2-9 (retrieval, rerank, generation, citations, eval, observability, CLI/CI, portfolio docs) sketched at block/chunk-list level, to be detailed once real chunk data exists. Wrote `docs/plans/2026-07-11-ask-my-docs-implementation-plan.md`. Not yet built — no code exists yet. | `uv` (not pip/venv) for Python + dependency management; `tiktoken` added as a plan-time library addition for token-accurate chunk sizing (not in original design doc's library table); `src/`-layout packages via pytest `pythonpath` config, no package install needed for dev; synthetic in-memory PDFs (via `fitz`) as test fixtures, never the real 273MB handbook | **Resume here next session: run `/build`** against `docs/plans/2026-07-11-ask-my-docs-implementation-plan.md`, starting with Chunk 0.1 (uv-managed environment bootstrap) |
| 2026-07-11 | Ran `/build` for Block 0 + Block 1 (all 15 planned TDD chunks, RED-GREEN-COMMIT each, 27 tests all green). Then ran Chunk 1.14's manual spot-check against the real 406-page handbook and found the subsection-header heuristic materially broken: the FAA handbook bolds inline field labels mid-sentence ("**Rule #1:** If you want to..."), which the bold-only heuristic flagged as headers, fragmenting the corpus into 1,850 chunks with a 29-token median (target 400-600; 60% of chunks under 50 tokens). Fixed via proper TDD (reproduced on a synthetic fixture first): `TextSpan` now tracks whether it's the sole content of its PDF line, and only sole-line spans can be headers. Re-ingested: 1,850 → 1,008 chunks, median token count 29 → 173.5. Also fixed a chapter-title misattribution bug as a side effect. Two more real findings logged to `BUGS.md` (not fixed this session): the sliding-window fallback sizes by word count as a token-count proxy, which breaks badly on numeral-dense TOC/Glossary text (confirmed: a 600-word chunk measured 1,275 actual tokens); and front/back-matter (TOC, Glossary, Index) sections have near-zero body text and raise an open scope question — filter them out of the retrieval corpus, or leave them in. | Sole-line-placement is now required for subsection-header detection, not bold/size alone — found and fixed via the plan's own guidance ("a new RED test against a synthetic fixture reproducing the failure, not a manual patch") | Block 1's 15 planned TDD chunks are done; two follow-up fixes are logged in `BUGS.md` (token-accurate windowing; front/back-matter filtering decision) but not yet built. Next session: either address those two BUGS.md items with proper TDD, or move to `/plan`-ing Block 2 (hybrid retrieval) now that real chunk data/schemas exist. |
| 2026-07-11 | Ran `/audit`: full suite green (28 tests); spawned a fresh-eyes subagent (no session context, only the changed files) to review Block 0+1 code for entropy. Found and fixed one Blocking issue via TDD — `apply_sliding_window` took a `min_tokens` parameter it never referenced, so a long section's trailing window could be an arbitrarily tiny orphan fragment; now merges under-floor trailing windows into the previous one. Fixed two Improvements — deduplicated a `_chunk()` test helper copy-pasted across `test_bm25_index.py`/`test_vector_index.py` into a shared `make_chunk` conftest fixture, and added a one-line comment explaining the non-obvious `figure_refs` sequence==0 gating. Logged one more Improvement (triple-duplicated groupby pattern, not extracted — a generic helper across 3 different value shapes risks premature abstraction) and 3 Nitpicks (acknowledged, not fixed) to `BUGS.md`. Also wrote and got approval on `docs/plans/2026-07-11-ask-my-docs-block2-retrieval-plan.md` (Block 2: hybrid retrieval, RRF fusion + orchestrator, deliberately lean since a reuse audit found Block 1 already has everything retrieval needs to search). Ran `/kaizen`: applied 2 concrete workflow edits — `.agent/workflows/build.md` now calls for verifying third-party library APIs with a throwaway script before writing RED tests against them (caught 3 real API mismatches this session: pydantic-settings' YAML source, typer's single-command collapse, PYTHONPATH not propagating outside pytest); `.agent/workflows/plan.md` now calls for sampling real source-document excerpts before writing synthetic fixtures for any heuristic-based extraction logic (would have caught the inline-bold bug at Chunk 1.3 instead of Chunk 1.14). Created `CHANGELOG.md` (first one for this project). | Audit's fresh-eyes subagent review (zero session context, changed-files-only) surfaced a real bug (`min_tokens` dead parameter) that wasn't caught by any of Block 1's own tests, since every test asserted on things the bug didn't affect | 28 tests green, all commits pushed to local history (no remote configured). Block 2's plan is written but **not built** — next session: run `/build` against `docs/plans/2026-07-11-ask-my-docs-block2-retrieval-plan.md`. |
| 2026-07-11 | Ran `/build` for Block 2 (hybrid retrieval) against `docs/plans/2026-07-11-ask-my-docs-block2-retrieval-plan.md`. Chunk 2.1 (`reciprocal_rank_fusion`, pure function, 4 tests) and Chunk 2.2 (`hybrid_retrieve` orchestrator over Block 1's existing BM25/vector search, 2 slow-marked tests against real indexes in `tmp_path`) built RED-GREEN-COMMIT; full suite 34 green. Chunk 2.2's planned weight-flip fixture (keyword "balance") turned out not to discriminate — the embedding model also ranked the keyword-stuffed chunk first — so per the plan's own Step 4 contingency it was replaced at RED time with a probe-verified fixture (semantic stall description vs off-topic keyword-"stall" chunk; margins BM25 0.46-vs-0.0, vector 0.70-vs-0.60). Chunk 2.3 real-corpus spot-check: fusion weights are load-bearing end-to-end (flipping 5.0/0.1 ↔ 0.1/5.0 changes the #1 result on 2 of 3 sample queries) — design-doc acceptance criterion met. Spot-check also produced the first retrieval-quality evidence on Block 1's two deferred questions: back-matter/index chunks pollute top-5 results (once as #1), and a new finding — V-speed subscripts split during extraction ("V" + "MC"), so the VMC/VSO sample query surfaced no defining chunk under any weighting. Both logged to `BUGS.md` as ingestion-side fixes needed before Block 6 evals. | Retrieval layer is deliberately thin (no wrapper modules over Block 1 search functions, per plan's reuse audit); test fixtures for model-dependent assertions get probe-verified against the real model before the test is finalized | Block 2 built and committed (34 tests green). Next: `/audit` Block 2, then either `/plan` Block 3 (rerank) or address the two pre-Block-6 ingestion items in `BUGS.md` (V-speed subscript joining, front/back-matter filtering). |
| 2026-07-12 | Fixed both pre-Block-6 ingestion items from `BUGS.md`, probe-first then TDD. (1) V-speed subscripts: verified against real span data that PyMuPDF splits "V"+"MC" into separate spans *without* setting its superscript flag, so `_join_subscript_spans` (`pdf_loader.py`) merges geometrically (<0.8× size, ≤1pt x-gap, shifted down); bare-"V" spans 100 → 37, "VMC" intact in 62 spans. (2) Front/back matter: probing real chunks showed the problem was worse than retrieval pollution — TOC pages repeat "Chapter N:" lines verbatim (every chapter header detected twice) and Index/Glossary headings fragmented back matter into ~330 junk sections (chapter 18: 402 of 951 chunks). Added config-driven `chunking.body_page_start`/`body_page_end` filtering spans *before* header detection; 21–390 for this corpus (Glossary kept as real definitional content, front matter + Index dropped). Also isolated the CLI tests from the repo's `config.yaml` (they now chdir to a tmp dir with a minimal config — the corpus-specific page range would have filtered synthetic test PDFs to nothing). Re-ingested and rebuilt both indexes: 951 → 617 chunks, median tokens 173.5 → 329, sub-50 share 33% → 8%. Spot-check re-run: VMC/VSO query now surfaces real VMC content (was: nothing relevant under any weighting), zero index/TOC chunks in any top-5, weight-flips still reorder. 39 tests green. | Subscript join is geometric because PyMuPDF's superscript flag is simply absent on this corpus's subscripts; body page range lives in `config.yaml` (corpus fact, not code); glossary is retrievable content, index/TOC are not | Both `BUGS.md` items checked off (word-count-proxy windowing remains open: 143 of 617 chunks >650 tokens). Next: `/audit` this work + Block 2, then `/plan` Block 3 (rerank). |
| 2026-07-13 | Ran `/audit` on Block 2 + the two ingestion fixes (everything since the Block 0+1 closeout). Full suite green (39 → 40 tests); fresh-eyes subagent (changed files only, no session context) returned 17 findings. Fixed via TDD: `reciprocal_rank_fusion` used `zip` without `strict=True`, so a weights list shorter than the rankings silently dropped a whole ranking — hybrid retrieval would quietly degrade to single-index with no error; none of Block 2's 4 fusion tests probed this. Also fixed 4 clarity items (a factually-false windowing comment written during the *previous* audit's fix, untyped `figure_refs_by_page`, unexplained no-op Typer callback, undocumented post-merge `line_span_count` semantics). Logged 7 Improvements to `BUGS.md` (notable: `config.yaml` loads cwd-relative — CLI run outside repo root silently skips the body-page-range filter; `hybrid_retrieve` reloads the BM25 index per query — wrong lifetime for Block 4's serving path). Audit **PASS**. Ran `/kaizen`: 4 workflow edits (net −9 lines) — `audit.md`'s checklist was another stack's template (npm/Vitest/Supabase/React, a `NEXT.md` grep, and a pointer to a nonexistent `.agent/REFERENCE.md`); rewrote it stack-native with new Contract Check and Comment Integrity items; `build.md` gains a contract-RED-test step (every chunk must test one contract case, prefer fail-loud APIs) and a config-isolation rule for corpus-specific config keys; dropped inapplicable JSX/UI bullets. Ran `/closeout`. | RRF fails loudly on rankings/weights mismatch rather than silently truncating; audit/build checklists now encode this repo's actual failure classes (silent-contract bugs, false comments, corpus-config leakage) instead of an inherited template's | 40 tests green. Next: `/plan` Block 3 (cross-encoder rerank) — real chunk/retrieval data now exists to design against. Open debt worth scheduling: token-accurate windowing (143 of 617 chunks >650 tokens) before Block 6 evals. |
| 2026-07-13 | Same day, second session: backfilled `LEARNING_NOTES.md` (6 missed session entries; file is now a required `/closeout` artifact wired into the workflow + CLAUDE.md — it's the user's primary learning artifact and had silently gone stale because no workflow named it). Then full Block 3 lifecycle: `/plan` (5 TDD chunks, reuse audit found chunk text already in the LanceDB table), `/build`, `/audit`, `/kaizen`. Built: `get_chunk_texts` (id→text on the existing table, fail-loud KeyError) and `rerank()` (`src/rerank/cross_encoder.py`) — config-named cross-encoder, candidates as `(id, text)` pairs, `enabled=False` passthrough that provably never loads the model. API probes verified LanceDB's no-vector filter and CrossEncoder's contract before RED tests, and probe-verified the discrimination fixture (rel −8.69 vs off −9.97). Spot-check exceeded the acceptance criterion (rerank toggle changed top-5 on all 3 queries; keyword-soup chunk demoted off VMC/VSO top-5) and found two real bugs: **duplicate chunk IDs** (617 rows / 590 unique; `make_chunk_id` collides on repeated section titles like "Common errors..." — one ID spans 5 different chunks; HIGH, must fix before Block 5) and **rerank latency ~5.3s/20 real chunks** vs the ~130ms design claim (claim only holds for short passages; probe on toy strings had "confirmed" it). A `.limit()` added as untested insurance became a silent-truncation bug under duplicate IDs — caught by the fail-loud KeyError on first real-corpus contact, fixed via TDD. Audit PASS (12 findings: 5 fixed incl. SQL-escaping IDs after the Comment Integrity check exposed an unenforced safety claim; 2 logged; 5 acknowledged). Kaizen: 4 edits (untested-defense rule, production-shaped perf probes, corpus-invariant counts in audit, Grep-tool note). | Candidates as pairs makes length mismatch unrepresentable (structural > runtime guard); disabled rerank keeps the top_k contract stable for downstream; designed invariants must be *counted* somewhere — "ids are unique" was false for two blocks because nothing ever compared row count to unique count | 49 tests green. Next: fix the HIGH duplicate-chunk-id bug (hash disambiguator + re-ingest + invariant check), then `/plan` Block 4 (grounded generation) — which also inherits the rerank-latency decision and the deferred serving-path debt items. |
| 2026-07-13 | Third session same day: fixed the HIGH duplicate-chunk-id bug, TDD throughout. `page_index_start` joined the `make_chunk_id` hash (chosen over an occurrence counter for stability under header-detection changes), and `chunk_pdf` now *counts* the uniqueness invariant, raising on collisions. That check fired on its first real-corpus run and exposed the audit's "latent" text-equality bug as real: glossary V-speed notation leaves stray standalone letter spans matching same-page alphabet headings ("V" ×5 spurious sections on p388). Fixed by matching headers on (title, font_size, is_bold) per page. Re-ingested and rebuilt both indexes: 617 → 612 chunks, **612/612 unique ids in both indexes, id sets identical** (first pass of the new corpus-invariant check). Spot-check re-run: retrieval stable; a previously-colliding "Common errors ... crosswind" chunk now distinctly retrievable (reranked #3 for the crosswind query). Both BUGS.md items closed with evidence. 53 tests green (49 → 53). Session ended before `/audit`. | Fail-loud guards chained three deep (KeyError → limit bug → duplicate ids → collision check → text-equality bug); disambiguator choice = "what stays invariant under likely future edits", not "what's easiest to compute" | **Resume here next session: `/audit` the chunk-id fix session** (commits `5820eb1`, `4af444f`, `6ab2732` + this closeout — small scope, changes are TDD-covered and invariant-verified), then `/plan` Block 4 (grounded generation; needs real `ANTHROPIC_API_KEY`, inherits rerank-latency + serving-path debt decisions). |
| 2026-07-13 | Fourth session same day: ran `/audit` on the chunk-id fix (commits `5820eb1`, `4af444f`, `6ab2732`). Full suite green (53/53). Independently re-verified the corpus invariant rather than trusting the prior session's log: 612/612 unique ids in both BM25 and LanceDB indexes, identical id sets. Re-ran the real-corpus spot-check: crosswind "Common errors" chunk retrievable at rank 3, VMC content surfaces cleanly, no TOC/index pollution. Fresh-eyes entropy subagent (changed files only, no session context) returned 0 Blocking, 2 Improvement, 2 Nitpick — logged the two Improvements to `BUGS.md` (the uniqueness check should be its own helper, not inlined in `chunk_pdf`; the write-side raise and read-side "arbitrary row wins" fallback for duplicate ids have no shared reference tying them together). Audit **PASS**, no code changes needed. User chose a light closeout (skip `/kaizen` — nothing surfaced worth a workflow edit) over the full lifecycle, given the clean result. | Two additional Low debt items logged; no code changed | 53 tests green. Next: `/plan` Block 4 (grounded generation) — needs real `ANTHROPIC_API_KEY` (currently placeholder-only), inherits the rerank-latency and serving-path debt decisions from Block 3. |
| 2026-07-13 | Fifth session same day: ran `/plan` for Block 4 (grounded generation). Wrote `docs/plans/2026-07-13-ask-my-docs-block4-generation-plan.md` (6 TDD chunks + spot-check), full detail per the project's scoped-planning convention. Reuse audit caught that `GenerationConfig.model` (`claude-sonnet-4-5`) is stale against the current model catalog — fix folded into Chunk 4.1 rather than left as another "verify at build time" IOU. Designed the citation-generation call around Claude's structured outputs (`messages.parse(output_format=...)`) instead of hand-parsed JSON, and SDK-native retry/timeout (`client.with_options(max_retries=, timeout=)`) instead of a hand-rolled `backoff_base_seconds` — the latter would have been dead configuration, the exact bug class this project's audits keep catching. Resolved Block 3's "double text fetch" debt note: the orchestrator reuses one `get_chunk_texts` call for both rerank's candidates and the final generation prompt, since rerank's output ids are a subset of its input ids — no change needed to rerank's already-shipped contract. Added a `live_api` pytest marker (skipped unless `RUN_LIVE_API_TESTS=1`) — Block 4 is the first block whose tests touch the real, paid Anthropic API, a gate Block 1's plan explicitly deferred to whenever that happened. Not yet built — no code changed this session. | Explicitly disable extended thinking (`thinking={"type": "disabled"}`) for the citation task — adaptive thinking is Sonnet 5's default when omitted, and a short extraction-and-cite task doesn't need it | Plan written, not built. Next: get a real `ANTHROPIC_API_KEY` into `.env` (Chunk 4.0 prerequisite for Chunks 4.3+), then `/build` against `docs/plans/2026-07-13-ask-my-docs-block4-generation-plan.md` — Chunks 4.1-4.2 (prompt template, schema, empty-context contract) need no API access and can start regardless. |
| 2026-07-13 | Sixth session same day: user obtained a real `ANTHROPIC_API_KEY`; ran `/build` for all 6 Block 4 chunks against `docs/plans/2026-07-13-ask-my-docs-block4-generation-plan.md`. Live-API sanity check confirmed the key first. Built RED-GREEN-COMMIT throughout: `live_api` pytest gate; versioned prompt + `build_prompt` (also fixed the stale `claude-sonnet-4-5` default to `claude-sonnet-5`); `GeneratedAnswer` schema + empty-context short-circuit that never touches the client; real API call via `client.messages.parse(output_format=...)` with SDK-native retry/timeout and `thinking` disabled (probe-verified against the real API first); `answer_question` orchestrator. Two real bugs surfaced by live-API tests and the Chunk 4.6 real-corpus spot-check (all fixed via TDD): (1) the model listed a chunk in the structured `citations` field even while explaining that chunk didn't answer the question — fixed by an explicit citations-field rule in the prompt, re-verified with no regression on the in-scope case; (2) the 8-question spot-check crashed on question 6 ("upset prevention and recovery") — a detailed answer exceeded `max_tokens=1024`, truncating the model's JSON mid-string and raising a raw `pydantic.ValidationError` three SDK frames deep; reproduced on a synthetic truncated-JSON fixture, then fixed by catching the error and raising a clear `RuntimeError` naming `max_tokens`, and by raising the default to 2048. Full spot-check re-run clean afterward: 7/8 questions cited correctly, question 8 (out-of-scope helicopter autorotation) returned an honest non-answer with empty citations. One retrieval-quality gap found and logged, not fixed: question 3 (short-field vs. soft-field takeoff) answered only the short-field half — a rerank surfacing issue, not a generation bug, flagged as input to Block 6's golden dataset. 62 tests green (53 → 62, 2 of them `live_api`-gated). Session ended before `/audit`. | Real-API tests and real-corpus spot-checks keep finding bugs neither synthetic fixtures nor fast unit tests could — a dead-simple prompt instruction gap and an untested `max_tokens` ceiling, both invisible until the actual model actually answered actual questions | **Resume here next session: `/audit` this build** (structured-output error handling, prompt-fix commit, max_tokens bump, orchestrator) — then either address the Q3 retrieval gap or move to `/plan` Block 5 (citation verification), which inherits the token-usage-capture debt item for Block 7. |
| 2026-07-13 | Seventh session same day: ran `/audit` on the Block 4 build. Full suite green (61 passed + 2 `live_api`, 63 total — one test added this session). Fresh-eyes entropy subagent found one real Blocking issue: `config.example.yaml`'s `generation:` section was never updated when `config.yaml` changed this block — still had the stale `claude-sonnet-4-5` model name and the dropped `backoff_base_seconds` field, missing `max_tokens`/`timeout_seconds`. Fixed by syncing it. Also fixed one Improvement (deduplicated the no-context answer string, previously hardcoded in `client.py` while every other piece of assistant-facing copy lives in `prompts/` — moved to `generate.prompt.NO_CONTEXT_ANSWER`) and, independently, found and closed a genuine audit gap: `answer_question`'s empty-retrieval short-circuit is load-bearing (confirmed by direct probe: `get_chunk_texts([])` crashes with an opaque LanceDB SQL-parser error on `WHERE chunk_id IN ()`) but had zero test coverage — added one, which passed, confirming the guard already worked correctly. Logged 3 more Low debt items (fake-client scaffolding duplicated across two tests; `generate_answer` doing three jobs in one function; `PROMPT_VERSION` currently unconsumed, both acknowledged as acceptable for now). Audit **PASS**. Ran `/kaizen`: 4 workflow edits — broadened `build.md`'s production-shaped-probe rule to cover LLM instruction-following claims (not just latency), broadened its "untested defenses" rule to cover guards that are provably *necessary* and not just speculative, and added two new `audit.md` checklist items (Guard Coverage, Config Template Sync) for the two failure classes this audit just found that no existing checklist line asked about. Then ran `/closeout`, which surfaced that `closeout.md` itself is the same kind of stale, inherited generic template `audit.md` used to be (npm scripts, `PROJECT_ROADMAP.md`, phase branches, GitHub Actions secrets, `/sync-workflows` — none of which exist or apply here); followed CLAUDE.md's actual described lifecycle (learning notes → changelog → project history → commit) instead, matching every prior closeout in this repo's history, and flagged `closeout.md` as a future `/kaizen` target rather than silently deviating. | A file only a human reads (an example config template) is invisible to every automated check — it needs its own explicit audit-time diff, because nothing else fails on its behalf; a guard being *correct* and a guard being *tested* are different properties, and only a test protects the first one from a future "looks redundant" deletion | 63 tests green. Next: either fix the Q3 retrieval-quality gap (short-field/soft-field takeoff answered only half the comparison) or `/plan` Block 5 (citation verification) — which inherits the token-usage-capture debt item for Block 7, and whichever happens first, `closeout.md` itself is due for the same stack-native rewrite `audit.md` already got. |
| 2026-07-13 | End-of-session decision (no code changes, no new commits this turn): reviewed overall project status (5 of 9 planned blocks done — env/config, ingestion, hybrid retrieval, reranking, and generation are all built and working end-to-end; citation verification, eval harness, observability, CLI/CI, and portfolio deliverables remain). Decided to defer the Q3 retrieval-quality gap (short-field/soft-field takeoff) rather than fix it ad hoc — it's better addressed once Block 6's retrieval metrics exist to confirm an actual improvement rather than blind tweaking. | Fixing retrieval/rerank without quantitative metrics risks trading one query's regression for another's fix with no way to tell — exactly why Block 6 (eval harness) exists | **Resume here next session: `/plan` Block 5** (citation verification — per-claim faithfulness check, coverage scoring, low-confidence flagging on Block 4's `{answer_text, citations}` output). `closeout.md` is still due its stack-native rewrite whenever there's a natural moment for it. |
| 2026-07-14 | Ran `/plan` for Block 5 (citation verification), then `/build` for all 6 chunks against `docs/plans/2026-07-14-ask-my-docs-block5-citations-plan.md`: judge schemas + versioned prompt (`prompts/verify_v1.md`), `CitationConfig` fields + empty-citations short-circuit, batched real judge call (`verify_citations` — one structured-output call scores every citation via `list[CitationVerdict]`) with fail-safe stripping of missing/unexpected verdicts and a clear `RuntimeError` on truncated output, a live-API test proving the strip path on an adversarial fixture (`stall001` kept, `unrelated1` stripped, coverage 0.5), and the `answer_with_verified_citations` orchestrator composing Block 4's `answer_question` with `verify_citations`. **Session was interrupted by an unplanned laptop shutdown mid-build.** Recovery needed no reconstruction: git history showed all 5 planned commits already landed exactly as the plan specified, and the full suite was still green (72 passed, 3 skipped) — the only uncommitted state was the plan file's checkboxes and Chunk 5.6's already-written spot-check narrative. Verified nothing was lost, then finished the session: real-corpus spot-check confirmed (all 8 design-doc questions, `coverage=1.0` across the board — Block 4's own prompt fix already filters most bad citations upstream, so the fail-safe strip path is proven only by Chunk 5.4's adversarial test, not organically here), question 3 (short-field/soft-field takeoff) answered both halves this run — logged as retrieval variance, not a confirmed fix — and closed out: `LEARNING_NOTES.md`, `CHANGELOG.md`, `BUGS.md` updated, plan file's success criteria committed. | Fail-safe default for a *missing* judge verdict (treat as unsupported, same as an explicit "no") is a different guarantee than "the judge said no" — matches the RRF/`get_chunk_texts` fail-loud pattern already established in this project | 75 tests total, 72 green + 3 gated (`live_api`/`slow`, skipped by default). Block 5 done — 6 of 9 planned blocks built. Next: `/plan` Block 6 (eval harness — golden dataset, retrieval/generation/citation metrics, CI quality gate). Block 6 directly inherits two open items: the Q3 retrieval-variance question (needs a metric, not another spot-check) and the token-usage-capture debt for Block 7's cost calculator. |
| 2026-07-14 | Ran `/plan` then `/build` for all 8 chunks of Block 6 (evaluation harness) against `docs/plans/2026-07-14-ask-my-docs-block6-eval-plan.md`. User chose (recommended options both times): ground truth via LLM-labeling against a wide 25-candidate pool + user review (not fully manual or skipped); answer-quality judge scores `correct`+`complete` as independent booleans (not correctness-only or a third groundedness dimension redundant with Block 5's `coverage`). Built: golden dataset schema + the 8 seeded design-doc questions; retrieval metrics (Recall@k/MRR/nDCG, pure functions); relevance-labeling judge + real ground-truth run (labeled all 8 questions' `relevant_chunk_ids`, hand-authored `reference_notes`, user-reviewed and approved); answer-quality judge (`judge_answer`) with a live-API discrimination test; sqlite response cache; git-commit-tagged baseline storage with tolerance-band comparison; `run_eval` orchestrator. Chunk 6.3's ground-truth labeling caught a real corpus gap: q4 (crosswind takeoff errors) has no "common errors" bulleted list anywhere in the corpus, confirmed by auditing the full candidate pool, unlike every comparable maneuver. The real end-to-end run (Chunk 6.8) found a second, more serious issue on its very first execution: q4's *generated answer* fabricated an 8-item "According to the handbook" errors list not present in its single cited chunk — caught by the answer-quality judge directly, a failure mode Block 5's citation verification structurally cannot catch (the cited chunk is genuinely relevant; the fabrication is in the prose, not an unsupported citation). Also found and fixed via TDD mid-session: `mrr` was missing the empty-relevant-ids vacuous-truth guard `recall_at_k`/`ndcg` both have (q8, deliberately zero relevant docs, scored `mrr=0.0` instead of `1.0`) — caught because the number looked wrong by eye on the first real run. That fix exposed a second real gap: the eval cache's `config_hash` only hashes `Settings` fields, so the code fix didn't invalidate the stale cached q8 result; worked around by clearing the cache manually before the corrected re-run, logged as unfixed debt. First real baseline saved: `mean_recall_at_k=0.616, mean_mrr=0.906, mean_ndcg=0.783, mean_coverage=1.000, correctness_rate=0.750, completeness_rate=0.500`. Also directly observed generation non-determinism: q1 (VMC/VSO) scored `correct=True` on the first run and `correct=False` on an immediate re-run of the identical question — concrete evidence for the design doc's tolerance-band (not hard-cutoff) CI comparison choice. 94 tests green, 4 gated (`live_api`/`slow`, skipped by default) — up from 75 green + 3 gated at Block 5's close. Session ended before `/audit`. | LLM-labeled-then-human-reviewed ground truth avoids both circularity (pure auto-derivation from the system under test) and unreasonable manual cost; a verification layer's stated scope limitation (Block 5's "citations only, not full prose faithfulness") is a prediction about the exact failure mode that will eventually surface, not just a caveat | **Resume here next session: `/audit` this build** (eval harness code + the mrr fix + the ground-truth labeling), then decide whether to fix the q4 fabrication bug and/or pin `generation.temperature` before `/plan`-ing Block 7 (observability) or Block 8 (CLI/CI — which is what actually wires `run_eval`/`compare_to_baseline` into a CI gate). |
| 2026-07-15 | Ran `/audit` on Block 6 (eval harness), which had ended last session before auditing. Full suite green (94 passed, 4 gated) going in. Found and fixed via TDD: (1) `eval.pipeline._evaluate_one` retrieved+reranked directly for retrieval metrics, then called `answer_with_verified_citations`, which retrieved+reranked *again* internally to generate the answer — doubling the confirmed ~5.3s/query real-corpus rerank cost per golden question, invisible in mocked tests, and directly relevant since this harness is what Block 8 wires into CI. Reproduced with a RED test asserting `hybrid_retrieve`/`rerank` are each called exactly once per question, then fixed by calling `generate_answer`/`verify_citations` (the primitives) directly instead of the composed function, reusing the already-computed retrieval. (2) The fresh-eyes entropy subagent found `judge_answer`/`label_relevance` duplicating the entire structured-judge-call boilerplate (identical `EvalConfig` field access, same five `parse()` kwargs, same error translation) — extracted to `call_structured_judge` (`src/eval/llm_call.py`). (3) Their test fakes (`FakeClient`/`FakeScopedClient`/`FakeMessages`) had been hand-rolled 5 more times across `test_judge.py`/`test_relevance.py`, crossing this project's own "revisit if a third fake-client test appears" threshold (set during Block 4's audit, never re-checked) — extracted a shared `FakeStructuredClient` fixture to `tests/eval/conftest.py`. Re-ran the fresh-eyes subagent on the updated files: no Blocking findings remained. 4 lower-priority findings logged to `BUGS.md` (config-shape duplication across `GenerationConfig`/`CitationConfig`/`EvalConfig`, `_METRIC_FIELDS`' silent-drop risk, `_evaluate_one`'s six-jobs-in-one-function shape, `_TEMPLATE_PATH`'s now-twice-duplicated relative-path construction). Docs/corpus checks clean (no stale status markers; Block 6 didn't touch ingestion/retrieval so no spot-check re-run needed). Audit **PASS**, 95 tests green (94 → 95, one new regression test). Ran `/kaizen`: 3 edits targeting this session's actual friction — `plan.md` gained a Composition Cost Audit bullet (does composing an existing pipeline function duplicate an expensive step already computed earlier in the same task?), `build.md` gained a Threshold Check bullet (check `BUGS.md` for existing "revisit at Nth occurrence" notes before writing another instance), `audit.md` gained a matching Composition Cost Check as a backstop. Also logged `closeout.md`'s still-stale generic template to `BUGS.md` properly (previously only mentioned in this file's prose, twice, and silently re-deferred both times) rather than let a third session repeat the same workaround unlogged. Followed CLAUDE.md's actual closeout lifecycle (this file, not the stale template). | Reuse of an existing composed function is only actually cheap if that function's own inputs don't overlap with work already done by the caller — otherwise "don't reimplement it, call it" quietly becomes "compute it twice"; a debt note that says "revisit at N" is only as good as whatever process actually checks for N, which is why it's now a checklist bullet, not just a `BUGS.md` line | 95 tests green. Next: `/plan` Block 7 (observability — Langfuse tracing) or Block 8 (CLI/CI, which is what actually wires `run_eval`/`compare_to_baseline` into a gate) — both still inherit the HIGH q4 generation-fabrication bug, the `generation.temperature` non-determinism decision, and the token-usage-capture debt item, all open in `BUGS.md`'s Eval section. `closeout.md`'s stack-native rewrite is now a proper tracked item, not just recurring prose. |
| 2026-07-15 | Second session same day: ran `/plan` then `/build` for Block 7 (observability). Plan (`docs/plans/2026-07-15-ask-my-docs-block7-observability-plan.md`) verified every Langfuse SDK claim against the real installed `langfuse==4.14.0` via throwaway scripts before writing chunk detail (context-manager span nesting via OTel context, the yielded span's own `.update()` method, `Message.usage.input_tokens`/`.output_tokens` sitting beside the already-used `.parsed_output`, safe client construction even with fake credentials). Surfaced two decisions to the user (both recommended options chosen): daily cost cap warns rather than blocks; eval judges get cost tracking only, no dedicated spans. Caught and fixed a real sequencing bug in the plan's own first draft before building — an early chunk referenced `hybrid_retrieve(..., observability=...)` before the chunk that adds that parameter, reordered leaf-function wiring before orchestrator-threading. Built all 9 non-blocked chunks (7.0-7.9) RED-GREEN-COMMIT: `Tracer`/`SpanHandle` protocols + `NoOpTracer`; `LangfuseTracer` + config-driven `get_tracer` factory; pure `calculate_cost` + config-driven price table (placeholder figures, not yet verified against real Anthropic pricing); sqlite daily running cost total + budget check; `ObservabilityContext` bundle + `report_usage`; traced `generate_answer`/`verify_citations`; traced `hybrid_retrieve` (3 spans: bm25/vector/fusion) and `rerank` (1 span); one shared `ObservabilityContext` threaded through `answer_question`/`answer_with_verified_citations` so a live query nests into one trace; cost-only wiring for `call_structured_judge` (both eval judges). Two real bugs caught and fixed within their own TDD cycles, never shipped: `report_usage` initially passed `ObservabilityConfig.cost_db_path` (a `str`) straight to `daily_cost.py`'s functions expecting `Path`, crashing on `.parent`; more seriously, `generate_answer`/`verify_citations` initially called `report_usage` unconditionally even under the no-observability default, which wrote real cost data to the real `data/daily_cost.sqlite3` on every test run (since `noop_observability()`'s default `ObservabilityConfig` still points at a real path with a real price table) — fixed by gating cost recording on whether `observability` was explicitly given, not on its defaulted value, matching the same guard already planned for the eval judges. 122 tests green (95 → 122), 4 gated. Chunk 7.10 (real end-to-end Langfuse trace verification) remains blocked — confirmed `LANGFUSE_PUBLIC_KEY`/`LANGFUSE_SECRET_KEY` still empty in `.env` — and is logged to `BUGS.md` along with the price-table-placeholder verification debt, per `build.md`'s Technical Debt Discovery step (not deferred to `/audit`). Session ended before `/audit`. | A "safe default" parameter isn't safe just because tests still pass — `noop_observability()`'s default config was a real, non-empty config, so "omit the parameter" silently meant "track cost against the real file with real prices," not "don't track cost"; the two are different guarantees and only testing file contents after a run (not just assertions) caught the gap | **Resume here next session: `/audit` this build** (9 chunks, ~15 files touched across `src/observability/` plus 5 already-shipped functions in Blocks 2-6), then decide whether to fix the still-open q4 fabrication bug / pin `generation.temperature` before `/plan`-ing Block 8 (CLI/CI). Chunk 7.10 stays blocked until the user sets up a free Langfuse Cloud project. |
| 2026-07-16 | Ran `/audit` on Block 7 (observability), which had ended two sessions ago before auditing. Full suite green (122 passed, 4 gated) going in. The technical checklist's new Additive-Parameter reasoning (grep every real caller of a function that gained a new cross-cutting parameter, not just the ones this block's own commits touched) found a real Blocking bug via TDD: `eval/pipeline.py`'s `_evaluate_one` — the harness's only production caller of `generate_answer`, `verify_citations`, and `judge_answer` — never passed the new `observability`/`observability_config` parameter to any of them, so every real Anthropic API call an eval run makes (generation + citation verification + judge, all real spend) silently never counted toward `daily_cost_cap_usd`, contradicting Block 7's own stated design-doc goal. Fixed: `run_eval` now builds one `ObservabilityContext` (matching the existing "construct once, thread down" convention) and threads it through all five calls, with two new regression tests. A fresh-eyes entropy subagent (changed files only, no session context) independently found the same bug plus a second real issue: the same hand-rolled `SpyTracer`/`_SpySpanCtx` test fake had been copy-pasted across 4 test files and 6 tests, well past this project's own "third occurrence" threshold — extracted to a shared `spy_tracer` fixture (`tests/conftest.py`); while there, also strengthened two existing span tests that captured `span.update()` calls into `update_calls` but never once asserted on the contents (a coverage illusion — the test name said "reports usage" but nothing checked that it did). Re-ran the subagent on the updated files: both fixes verified correct and complete, no new Blocking findings. One Blocking-tier DRY finding was deliberately deferred, not fixed: `generate_answer`/`verify_citations`/`call_structured_judge` all independently implement the same with-options/parse/catch-ValidationError sequence (3 sites, not identical — 2 open a tracer span, 1 deliberately doesn't per Decision 2) — logged to `BUGS.md` with a concrete fix template rather than forced through mid-audit, since unifying needs a shared config-shape decision first. Docs/corpus checks clean (no stale status markers; retrieval/rerank logic itself didn't change this phase, only additive tracer wrapping, so no spot-check re-run needed) — but did surface that the design doc's "each trace and eval run is attributable to a specific prompt version" acceptance criterion is only half-met (eval runs yes, via Block 6; Langfuse spans no), logged as new debt. Audit **PASS**, 126 tests green (122 → 126, 4 new/strengthened tests). Ran `/kaizen`: 4 workflow edits directly traced to this session's findings — `plan.md` gained an Additive-Parameter Reach Audit bullet (list every real caller when adding a cross-cutting optional parameter, don't just say "additive-optional"); `audit.md` gained a matching Additive-Parameter Reach Check as a backstop plus a new Spy Assertion Check (a test spy that records but never asserts is false coverage); `build.md`'s Threshold Check now also watches for hand-rolled test fakes repeating *within* the same build session, not just patterns already logged in `BUGS.md` from prior sessions. | A capability being fully unit-tested and a capability actually being reached by its real caller are different claims — "the tests pass" only answers the first one, and Block 7's own five-function rollout satisfied the first everywhere while silently failing the second at its highest-cost real call site | 126 tests green. Next: `/plan` Block 8 (CLI/CI — wires `run_eval`/`compare_to_baseline` into a gate, and is also the natural place to surface the daily cost total to a human). Still-open carried-forward items: the HIGH q4 generation-fabrication bug, `generation.temperature` non-determinism, the deferred 3-site LLM-call-boilerplate duplication, the price-table-placeholder verification, and Chunk 7.10's Langfuse trace verification (blocked on the user creating a free Langfuse Cloud project). |
| 2026-07-16 | Second session same day: ran `/plan` for Block 8 (CLI completion + CI) — the second-to-last planned block (Block 9, Portfolio Deliverables, follows it). Reuse audit found `query`/`eval` need almost no new orchestration (`answer_with_verified_citations`, `run_eval`, `compare_to_baseline`, `save_baseline`, `load_latest_baseline` already do the work), except one real gap: `run_eval` always makes real, paid Anthropic calls, so the design doc's "cheap retrieval gate, zero API cost, every push" acceptance criterion has no code path to run today. Surfaced three infra decisions to the user (all recommended options chosen): create a real GitHub remote and push this block (a CI workflow that never executes isn't verifiable and doesn't serve the portfolio audience); commit the built `data/index/` (~7.3MB) to git rather than rebuild it in CI from a re-downloaded 273MB PDF (the corpus isn't changing; a committed artifact avoids cache-invalidation design and an external-URL dependency); skip the stretch-goal `serve` command. Designed a `retrieval_only` mode for `run_eval`/`_evaluate_one` (additive-optional parameter, matching Block 7's established shape) that computes only `recall_at_k`/`mrr`/`ndcg` and skips generation/verification/judging entirely — `EvalResult`'s answer-quality fields become `Optional` (`None`, not a sentinel, when skipped, so "never computed" stays structurally distinct from "computed and failed"), `compare_to_baseline` skips `None`-valued fields so a `retrieval_only` run can compare against the same full-run baseline `nightly-eval.yml` writes, and the sqlite response cache is bypassed entirely in this mode (its whole justification — avoiding re-spending on paid calls — doesn't apply here, and extending its key adds real risk for no benefit). Wrote 6 chunks with full TDD detail to `docs/plans/2026-07-16-ask-my-docs-block8-cli-ci-plan.md`: a non-TDD infra prerequisite (GitHub remote + `.gitignore` narrowing + a clean re-ingest to drop stale multi-version LanceDB cruft before committing the index), `query` CLI command, the `retrieval_only` eval-harness change, `eval` CLI command (baseline compare + exit-code gate + a deliberately-regressed-fixture test proving the gate actually fails), `cheap-gate.yml`, `nightly-eval.yml`, and a final push-and-verify-both-workflows-actually-run chunk. Logged 6 plan-time decisions to `.agent/decisions.log`. Not yet built — no code changed this session. | GitHub Actions' `ANTHROPIC_API_KEY` requirement is asymmetric by design: `cheap-gate.yml` needs the env var *present* (pydantic-settings' `Settings()` has no default for it) but never *real* (a placeholder is safe since `retrieval_only` mode makes zero Anthropic calls), while `nightly-eval.yml` needs a real secret since it spends real money — the same config field has different trust requirements depending on which code path actually reads it | Plan written, not built. Next: `/build` against `docs/plans/2026-07-16-ask-my-docs-block8-cli-ci-plan.md`, starting with Chunk 8.0 (GitHub remote setup — needs the user's repo-name/visibility confirmation and possibly `gh auth login`). |
| 2026-07-17 | Ran `/build` for all 6 chunks of Block 8 (CLI completion + CI) against `docs/plans/2026-07-16-ask-my-docs-block8-cli-ci-plan.md`. Chunk 8.0: `gh auth status` already authenticated; user confirmed `ask-my-docs`/public; did a clean re-ingest (612 chunks, dropped 3 stale multi-version LanceDB data files down to 1) before narrowing `.gitignore` (`data/*` + `!data/index/**`) and committing the 2.4MB index; created and pushed to `https://github.com/gokuldilipkumar/ask-my-docs`. Chunks 8.1-8.3 built RED-GREEN-COMMIT: `query` command (wraps `answer_with_verified_citations`, verified against the real API on a tiny synthetic index — `live_api` test, not just mocked); the `retrieval_only` eval mode exactly as planned (`EvalResult`/`EvalRunResult` fields `Optional`, cache bypassed, `compare_to_baseline` skips `None`-valued fields — existing baseline JSON still loaded unchanged, confirming backward compatibility); `eval` command (baseline compare, exit-code gate, `--save-baseline` refused under `--retrieval-only`, 6 unit tests including the design doc's named "deliberately bad config fails the gate" case) — sanity-checked live against the real committed corpus index, exact match with the stored baseline, exit 0. Chunk 8.4 (`cheap-gate.yml`) committed and pushed immediately (deliberately ahead of the plan's own chunk ordering, to catch CI environment issues before compounding a second untested workflow) — **passed on its first real GitHub Actions run**: 126 fast + 11 slow tests + retrieval-only eval, all green, exact match with baseline. Chunk 8.5 (`nightly-eval.yml`) needed real troubleshooting: the first manually-triggered run failed with `httpx`/`h11` `LocalProtocolError: Illegal header value` surfacing as `anthropic.APIConnectionError`; traced to a literal leading space in `.env`'s `ANTHROPIC_API_KEY=` line (confirmed via `xxd` on the raw bytes) that `pydantic-settings`' dotenv parser silently tolerates locally but `gh secret set`'s raw shell pipe preserved byte-for-byte into the GitHub secret — the exact same nominal key value behaved differently across the two consumers of the same `.env` file. First fix attempt (`tr -d '\r\n'`, guessing CRLF) didn't touch it since the character was a literal space, not a line ending; second attempt (`xargs`, trims all whitespace) fixed it. Third trigger **succeeded for real**: all 6 metrics PASS (`correctness_rate` 0.75→0.875, `completeness_rate` 0.5→0.625 vs. the stored baseline — real generation/judge variance, not a regression), new baseline auto-committed and pulled back locally. Chunk 8.6 (push+verify) was folded into the above rather than run as a separate step. 137 tests green (126 → 137 local; the GitHub Actions run itself is now an additional real-infra check beyond local pytest). Checked off all 9 plan success criteria in the plan doc; logged the `.env`-leading-space root cause and closed the "no CLI surfaces daily cost" Block 7 debt item in `BUGS.md`. Session ended before `/audit`. | A value that parses identically through one library's lenient parser and literally through a raw shell pipe is not actually the same value in both places — `.env`'s dotenv-tolerant leading space was invisible through 4 blocks of local `live_api` tests and only became observable the moment a byte-literal consumer (`gh secret set`) touched the same file | 137 tests green, both CI workflows verified passing on real GitHub Actions infrastructure. **Resume here next session: `/audit` this build** (6 chunks: query/eval CLI, retrieval_only eval mode, two GitHub Actions workflows, GitHub remote + committed index), then `/plan` Block 9 (Portfolio Deliverables — README, architecture diagram, eval report; the final planned block). Still-open carried-forward items unchanged from Block 7's audit: the HIGH q4 generation-fabrication bug, `generation.temperature` non-determinism, the deferred 3-site LLM-call-boilerplate duplication, the price-table-placeholder verification, and Chunk 7.10's Langfuse trace verification (still blocked on the user creating a free Langfuse Cloud project). |
| 2026-07-17 | Second session same day: ran `/audit` on Block 8 (CLI completion + CI). Full suite green (137 passed, 5 skipped) going in. Technical checklist clean: re-verified the corpus invariant after Chunk 8.0's re-ingest (612/612 unique ids in both indexes, identical id sets, via a pyarrow-direct read since `pandas` isn't installed), confirmed every comment added this block factually accurate, confirmed no guard-coverage or additive-parameter-reach gaps (`retrieval_only` has exactly one real caller, the new `eval` command, correctly threaded), confirmed `git status` clean under the narrowed `.gitignore`. Fresh-eyes entropy subagent (changed files only, no session context, explicitly told to scrutinize the two GitHub Actions YAML files with the same rigor as Python) found one real Blocking-tier CI gap and one false positive. Real: `cheap-gate.yml` had no explicit `permissions:` block and no `concurrency` group despite triggering on both `push` and `pull_request` — a PR branch's every commit would fire two overlapping runs with nothing to cancel the stale one. Fixed: added `permissions: contents: read` + a cancel-in-progress concurrency group; added a non-cancelling concurrency group to `nightly-eval.yml` too (protects its real-cost baseline-commit step from a race between overlapping triggers). Both re-verified passing on real GitHub Actions after the fix. False positive: the subagent flagged "no ingest step, index probably uncommitted, both gates must fail" — confirmed wrong via `git ls-files data/` (all 13 index files tracked) and the fact that both workflows had already passed for real before the review ran; dismissed with that evidence, not on instinct. 3 Low findings logged to `BUGS.md`, not fixed (duplicated 2-line cost-reporting block between `query`/`eval_command`, under this project's "third occurrence" threshold; an empty-`compare_to_baseline`-dict edge case that's structurally unreachable while `mean_recall_at_k`/`mean_mrr`/`mean_ndcg` stay non-`Optional`; a pre-existing Block-6 `n = len(results) or 1` silent-zero for an empty reviewed-question set, never previously logged). Docs sweep clean (all "not yet built"/"pending" hits were correctly-dated historical narrative, not stale current-state markers); no real-corpus spot-check needed (retrieval/rerank logic itself unchanged this block). Audit **PASS**. Ran `/kaizen`: 3 workflow edits, all traced to this session's actual friction — `build.md` gained two bullets under Third-Party API Verification (a CI workflow or other infra artifact isn't done until pushed and observed running, not just read; never pipe a raw config/secret value into a remote secret store without stripping whitespace, since local parsers are commonly lenient in ways a byte-precise secret store isn't); `audit.md` gained a CI Workflow Correctness checklist item (permissions, concurrency, verified-not-just-read artifact paths) so future audits check workflow YAML systematically without a specially-worded subagent prompt each time. | A fresh-eyes review's "zero stake in the story already told" cuts both ways — it has no access to context the auditor already independently verified either, so a finding built on a false premise needs the same evidence bar to dismiss as a real one needs to fix; separately, a concurrency gap is a correctness property that's true by luck-of-timing until two runs actually overlap, not something any test or log would ever surface on its own | 137 tests green, both CI workflows passing on real GitHub Actions. Next: `/plan` Block 9 (Portfolio Deliverables — README, architecture diagram, eval report; the final planned block). Still-open carried-forward items unchanged: the HIGH q4 generation-fabrication bug, `generation.temperature` non-determinism, the deferred 3-site LLM-call-boilerplate duplication, the price-table-placeholder verification, and Chunk 7.10's Langfuse trace verification (still blocked on the user creating a free Langfuse Cloud project). |
| 2026-07-17 | Third session same day: ran `/plan` for Block 9 (Portfolio Deliverables — the final planned block). Reuse audit found the two real eval baselines (`eval/baselines/20260714T151052Z_dfac1522.json`, `20260717T095425Z_3c6a14e9.json`) and the already-shipped `eval` CLI's PASS/FAIL table need no new eval code — the eval report chunk only assembles real, already-committed data. Surfaced two decisions to the user (both recommended options chosen): provision a real Langfuse Cloud project this session and resume Block 7's blocked Chunk 7.10 (real trace + screenshot) as Block 9's first chunk, rather than documenting observability without a real trace; make the eval report's "caught regression" example a freshly-run real regression (`rerank.enabled: false` + `eval --retrieval-only` against the real corpus and real baseline, zero API cost) rather than reusing a historical bug narrative or the existing Chunk 8.3 unit-test fixture. Also decided (not surfaced as a question, stated directly in the plan): the architecture diagram is an inline Mermaid block in `README.md`, not a generated image; `docs/eval-report.md` is a separate file linked from the README, not inlined. Wrote 6 chunks to `docs/plans/2026-07-17-ask-my-docs-block9-portfolio-plan.md`: Langfuse setup + real trace verification/screenshot, the real regression run (captured then reverted, `config.yaml` ending unchanged), `docs/eval-report.md`, the Mermaid diagram (verified against real span names from the real trace, not just source-reading), `README.md` (every quickstart command run for real and its actual output captured, not hand-typed), and a final link/consistency polish pass. The plan's core discipline, stated explicitly in its Conventions Check: every number, command, and diagram node must be checked against something real (a run, a committed file, an actual screenshot) before being written down — named as this block's version of the audit pattern that already caught a stale `config.example.yaml` (Block 4) and an unverified price table (Block 7, still open). Logged 4 plan-time decisions to `.agent/decisions.log`. Not yet built — no code or docs changed this session; user is provisioning the Langfuse Cloud project now. | A portfolio document is the highest-visibility place for documentation/reality drift to recur, since Block 4's and Block 7's versions of that bug were both caught by internal audits a hiring-manager reader would never run — so this block's own working discipline had to be named as a first-class constraint, not left implicit | Plan written, not built. Next: `/build` against `docs/plans/2026-07-17-ask-my-docs-block9-portfolio-plan.md`, starting with Chunk 9.0 once the user has pasted real `LANGFUSE_PUBLIC_KEY`/`LANGFUSE_SECRET_KEY` into `.env`. |
| 2026-07-18 | Fourth session, continuing same day: ran `/build` for all 6 chunks of Block 9 (Portfolio Deliverables — the final planned block) against `docs/plans/2026-07-17-ask-my-docs-block9-portfolio-plan.md`. User provided real Langfuse Cloud keys; written into `.env` whitespace-stripped and byte-verified. Chunk 9.0's first real trace screenshot immediately surfaced a genuine HIGH bug invisible to Block 7's entire unit-test suite: all 6 pipeline spans were landing as **separate root traces**, not nested under one trace per query — confirmed via the Langfuse SDK's own docstring ("child of the current span in the context") that nesting requires an *active parent span*, which neither `answer_with_verified_citations` nor `answer_question` ever opened; sharing one `ObservabilityContext`/tracer instance (Block 7 Decision 6) was necessary but not sufficient. User chose to fix immediately via TDD rather than defer, and to fix the identical bug shape in `eval/pipeline.py`'s `_evaluate_one` (5 spans, same missing wrapper) in the same pass rather than leave a known twin unfixed. Fixed both by wrapping each call chain in one parent span (`query.answer_with_verified_citations`, `eval.question.<id>`); regression coverage added by extending the shared `spy_tracer` fixture to record enter/exit order, catching a missing wrapping `with` block structurally without needing a real backend. Re-ran the live test — real dashboard now shows one correctly-nested trace, screenshotted to `docs/images/langfuse-trace.png`. Chunk 9.1's planned regression demo (`rerank.enabled: false`) also didn't go as planned: it *improved* every retrieval metric instead of degrading them (reported honestly in the eval report rather than discarded); `retrieval.top_n: 20→3` degraded metrics but stayed inside the tolerance band; `retrieval.top_n: 20→1` produced a real, reproducible, deterministic gate failure (exit code 1), used as the real regression example instead. Chunk 9.2 (`docs/eval-report.md`) assembled from real baseline JSON and the regression run's real captured output. While capturing the README's own eval quickstart command (Chunk 9.4), a stale local eval cache (last touched 2026-07-14, never invalidated since) was caught returning 4-day-old judge verdicts with zero new spend; cleared it and re-ran fresh, which produced a second, live, unplanned instance of the same generation non-determinism finding from Block 6 — folded into the eval report as additional real evidence. `README.md` written with a Mermaid architecture diagram, real captured command output throughout, and the real trace screenshot; both confirmed rendering on the real pushed GitHub page (`https://github.com/gokuldilipkumar/ask-my-docs`) after WebFetch's own broken-image flag was checked and found to be a false positive (the raw content URL resolved with an exact byte-count match). Final polish pass found no link or cross-document metric drift. All 9 planned blocks are now built. 139 tests green (137 → 139, two new nesting-regression tests), both CI workflows still passing, `config.yaml` reverted clean. Session ended before `/audit`. | A portfolio deliverable session doing exactly what its own plan demanded — verify every claim against something real before writing it down — surfaced a real production bug (trace nesting), a wrong assumption (the planned regression didn't regress), and a stale-cache artifact, none of which any prior session's tests or audits had caught, because none of them had ever pointed a real Langfuse backend or a fully-fresh eval cache at this system before | 139 tests green. **Resume here next session: `/audit` Block 9** (and, since this is the final planned block, this audit effectively closes out the whole 9-block build) — still-open carried-forward items: `generation.temperature` non-determinism (now doubly evidenced), the deferred 3-site LLM-call-boilerplate duplication, the price-table-placeholder verification, and the newly-logged stale-local-eval-cache and Windows-console-encoding items. |
| 2026-07-18 | Fifth session, continuing same day: ran `/audit` on Block 9. Full suite green (139 passed, 6 skipped) going in. Manual checklist clean (no new function signatures, no new guards/parameters this block, no CI workflow changes, comment-integrity check passed on the two new nesting-fix comments, no stale status markers beyond the plan doc's own standard closing boilerplate). Fresh-eyes entropy subagent (changed files only, no session context, explicitly asked to cross-check every numeric claim in `README.md`/`docs/eval-report.md` against source files) found zero Blocking issues — confirmed the span-wrapping fix is the correct minimal shape, confirmed every test-count and metric-table claim checked out exactly against `config.yaml`/`eval/baselines/*.json`/git history. Two Improvements, both fixed immediately: `pyproject.toml`'s `description` was still the unedited `uv init` placeholder (fixed to a real one-line summary); `docs/eval-report.md`'s baseline table cited `20260717T095425Z_3c6a14e9.json` as "current," but `nightly-eval.yml` had auto-committed a newer file (`20260718T091928Z_f9eedb70.json`, merged into this session's branch from a remote push made overnight) with identical numbers — updated the table to the actual latest and added a note that the report is a snapshot, not a live pointer, since nothing ties it to whatever `eval/baselines/` sorts last as of any given day. One Nitpick (the nesting-fix rationale comment repeated near-verbatim across 4 files) acknowledged, not fixed. Audit **PASS**. Ran `/kaizen`: 5 workflow edits, all traced to this session's real friction — `build.md` gained three bullets (a tracing SDK's nesting behavior confirmed in isolation is not the same claim as the codebase actually reaching that behavior; verify a local cache is fresh, not stale, before trusting its output as representative for documentation; capture each documented number from its own dedicated fresh run, never reuse a figure across different commands — the exact mistake this session made and caught only via a lucky UTC day rollover); `plan.md` gained a Predicted-Behavior Claim Check (the rerank-disabled regression demo predicted the wrong direction entirely); `audit.md` gained a matching Trace Nesting Reach Check backstop. Then, since this is the project's final planned block and `closeout.md`'s stack-native rewrite had been deferred twice already (Block 4, Block 6) as "revisit at a natural moment" — this was that moment — rewrote it from the generic inherited template (npm/lint/build, `PROJECT_ROADMAP.md`, phase branches, `/sync-workflows`) to match this project's real artifacts and the lifecycle `CLAUDE.md` already described, with an explicit "what this project does not have" section so it doesn't silently regress back to the template's assumptions. All 9 planned blocks now built and audited. | The two Improvement findings were both instances of the same root cause — a fact written down once and never re-checked against a moving source of truth (an unedited scaffold string nobody revisited; a baseline file reference that assumed today's "latest" would stay latest) — while the Blocking-tier check (every real number cross-verified against its actual source file) came back completely clean, which is itself evidence that the session's own "verify everything against something real" discipline, applied consistently while writing the docs, is what kept real errors from reaching this audit in the first place | 139 tests green, audit PASS, all 9 planned blocks (env/config, ingestion, hybrid retrieval, reranking, generation, citation verification, eval harness, observability, CLI/CI, portfolio deliverables) built and audited. Still-open carried-forward debt (all in `BUGS.md`, none blocking): `generation.temperature` non-determinism (now evidenced three separate times across Blocks 6 and 9), the deferred 3-site LLM-call-boilerplate duplication, the price-table-placeholder verification, the stale-local-eval-cache and Windows-console-encoding findings from this session, and Chunk 7.10's real-trace verification is now fully closed. Next session: no more planned blocks remain — either address open `BUGS.md` debt, or the project moves into a maintenance/portfolio-polish phase at the user's discretion. |
| 2026-07-18 | Sixth session, continuing same day: user chose to close out the now-three-times-evidenced `generation.temperature` debt item rather than leave it open now that no planned blocks remain to compete with it. Fixed via TDD: `GenerationConfig.temperature` (new field, default `0.0`) wired into `generate_answer`'s `messages.parse(...)` call, matching the existing `citations.judge_temperature`/`eval.judge_temperature` pattern; `config.yaml`/`config.example.yaml` synced. Two new tests (`GenerationConfig` defaults to `0.0`; `generate_answer` passes the configured value through). 130 fast tests green (128 → 130). Light closeout (no full `/audit` cycle — small, well-tested, pattern-matching change) per `LEARNING_NOTES.md`/`CHANGELOG.md`/`PROJECT_HISTORY.md`/`BUGS.md` updated directly. User also asked how to try the built system themselves for the first time; pointed at the `query`/`eval` CLI commands, the Langfuse dashboard, and the GitHub Actions tab/README badges. | A debt item with its tradeoff already fully written down doesn't need more evidence to become decidable — it needs a session where deciding it is the point, not a side-finding competing against whatever else that session was building | 130 tests green. `generation.temperature` debt item closed. No more planned blocks remain; still-open non-blocking debt in `BUGS.md`: the deferred 3-site LLM-call-boilerplate duplication, the price-table-placeholder verification, the stale-local-eval-cache `config_hash` gap, and the Windows-console-encoding cosmetic finding. Next session: user-directed — either more `BUGS.md` cleanup or the project moves fully into portfolio-use/maintenance mode. |
| 2026-07-18 | Seventh session, continuing same day: the previous session's `generation.temperature` fix was wrong and broke the app. The user tried the `query` CLI for the first time (asked what commands to run — first-ever manual try of the built system), hit a real crash, and attached screenshots of the full traceback. Root cause, confirmed by a real probe against the live API: `claude-sonnet-5` rejects `temperature` outright (`BadRequestError`, "`temperature` is deprecated for this model"), and a follow-up probe found `top_p`/`top_k` are deprecated for this model too — it exposes no manual sampling-determinism control at all. The previous fix had been verified only against fake-client unit tests, never against the real API, despite `build.md` stating exactly that verification rule since Block 0 and having gained a sharper version of it during this same day's own `/kaizen`. Reverted via TDD: removed `GenerationConfig.temperature` and the API call, replaced the two now-wrong tests with one regression test asserting `generate_answer` never sends `temperature`/`top_p`/`top_k`, and this time actually ran the existing real-API `live_api` tests (`RUN_LIVE_API_TESTS=1`) before committing — both passed, confirming the revert works against production. Corrected the record rather than silently editing away the false claim: `BUGS.md`'s "Fixed" entry reopened with the real finding (this model currently has no fixable path to deterministic sampling; the eval harness's tolerance-band comparison is the actual mitigation, not a stopgap), `CHANGELOG.md` and `.agent/decisions.log` both got forward-correction entries, and `LEARNING_NOTES.md` got an honest entry on why the mistake happened (an already-proven parameter for one model, `claude-haiku-4-5-20251001`'s `judge_temperature`, was wrongly treated as proven for a different model, `claude-sonnet-5`, without re-verifying). 129 tests green. | The user manually running the CLI for the first time is exactly the kind of real-world check this project's own audits keep saying nothing else can substitute for — a fake-client test suite cannot reject a parameter the real API rejects, because the fake was built by the same reasoning that made the mistake in the first place | 129 tests green, real bug fixed and verified against the live API before commit, documentation record corrected forward (not edited away). No more planned blocks remain; still-open non-blocking debt unchanged except `generation.temperature` is now understood as likely unfixable via sampling params rather than merely unfixed. |
| 2026-07-21 | Scoped and built Block 10 (new addition, beyond the original 9-block plan): a Streamlit chat interface over `answer_with_verified_citations`. Wrote `docs/plans/2026-07-21-ask-my-docs-block10-chat-interface-plan.md`, then built it via 8 TDD chunks: probed `streamlit.testing.v1.AppTest`'s real mocking mechanism first (found it re-executes the whole script fresh per `.run()`, so tests must patch source modules like `citations.pipeline.answer_with_verified_citations`, not the re-bound names inside `streamlit_app.py` — a plan-time assumption that would have silently failed if untested); built `src/ingest/chunk_metadata.py` (a new `chunk_id -> {chapter, section, page}` JSON sidecar written at ingest time, resolving citations to human-readable text and finally closing the "raw chunk_id" item flagged in `BUGS.md` since Block 8); wired it into the `ingest` CLI and re-ingested the real corpus (612 chunks, confirmed zero retrieval-metric drift vs. the existing baseline before committing); built `src/app/streamlit_app.py` incrementally (chat input + history → resolved sources panel + low-confidence flag + spinner → sidebar daily-cost/budget-cap banner → pipeline-error resilience). Verified the whole thing for real via browser automation against the live corpus (real Anthropic API call, ~$0.0007): the Sources panel correctly resolved 4 citations to "Ch. 13: Transition to Multiengine Airplanes — Engine Inoperative Flight Principles" — and all 4 resolved identically, a real, now-observed consequence of the still-deferred `printed_page_label` gap, logged as a new `BUGS.md` finding rather than silently accepted. Full suite green (138 passed) throughout. Closed with README/`BUGS.md`/`.agent/decisions.log` updates. | Streamlit chosen over Gradio/FastAPI (chat_message + sidebar + expander primitives fit "answer + evidence panel," one-click Community Cloud deploy for the hiring-manager audience); citation metadata resolved via a new JSON sidecar rather than touching the tuned LanceDB schema; this is a chat-*styled* single-turn UI, not conversational memory (stated explicitly in the README); missing-metadata handling is fail-loud in the library, fail-soft in the UI | Chat UI is local-only for now — Streamlit Community Cloud deployment is a manual next step tied to the user's own account, not automated here. `printed_page_label` (bbox-based page-footer detection) remains the one open item that would make same-section citations distinguishable. |
| 2026-07-21 | Second session, continuing same day: ran `/audit` on Block 10. Full suite green (152 passed, 6 skipped). Verified corpus invariants for real (612 unique chunk ids, identical sets across BM25/LanceDB/`chunk_metadata.json`). Fresh-eyes entropy subagent (changed files only) found 2 Blocking issues, both fixed: `Path("data/index")` hardcoded independently in 3 places (extracted to `app.paths.DEFAULT_INDEX_DIR`), and a test named `test_load_chunk_metadata_raises_on_unknown_id` that never actually exercised any raising behavior (rewritten with a real `pytest.raises(KeyError)`, now pinning the exact contract `streamlit_app.py`'s fallback branch depends on). While drafting the README's Chat Interface section, caught myself writing a fabricated cross-reference ("see the Quickstart's src-layout note") that didn't exist — fact-checking it surfaced a real, pre-existing bug: the README's own Quickstart commands have been missing `PYTHONPATH=src` since before this block, copy-paste-broken exactly as shown. Fixed both, plus refreshed stale pytest counts (128/139 → 141/152). Also recognized the new Streamlit sidebar cost display as the third occurrence of a duplication `BUGS.md` had already flagged "revisit at a third occurrence" (Block 8) — extracted `observability.daily_cost.format_daily_cost` instead of logging a fourth mention. Ran `/kaizen`: 4 workflow edits — `build.md`'s Third-Party API Verification now explicitly covers UI/app testing harnesses (`AppTest` re-executes the whole script per `.run()`, so patches must target source modules); `audit.md` gained a Runnable Command Check (run README commands exactly as shown), a Doc Cross-Reference Check (verify a "see the X note" pointer actually exists), and an explicit BUGS.md-threshold check inside entropy-review triage. Then `/closeout`. | Fresh-eyes entropy review's value isn't just catching new code smells — it triggered a real self-check (the fabricated cross-reference) that had nothing to do with the subagent's own findings, purely because verifying one claim naturally led to verifying an adjacent one | Chat interface shipped and audited clean; local-only (Streamlit Community Cloud deploy is a manual next step). `printed_page_label` (bbox-based page-footer detection) remains the one open item that would make same-section citations distinguishable — next natural pickup if there's a Block 11. |
